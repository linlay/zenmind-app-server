# Release Guide

This project has moved to a container-only deployment flow.

- Deployment and operations are documented in [README](./README.md).
- JWK key management scripts are documented in [release-scripts/README](./release-scripts/README.md).

If you came from older packaging versions, use `docker compose` directly from the repository root now.
